import { TreeFilterPipe } from './tree-filter.pipe';

describe('TreeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TreeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});